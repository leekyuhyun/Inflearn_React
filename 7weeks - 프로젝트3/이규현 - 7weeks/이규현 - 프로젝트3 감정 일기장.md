# 📘 감정 일기장 (Emotion Diary)

> **인프런 - 한 입 크기로 잘라 먹는 리액트(React.js) : 기초부터 실전까지** > 강의의 지식을 바탕으로 구성한 최종 프로젝트입니다.

## 💡 학습 목표 및 성과

- 상세한 상태 관리: useReducer를 사용하여 복잡한 상태 업데이트 로직을 깔끔하게 분리했습니다.

- 최적화 및 UX: useEffect를 활용한 데이터 동기화와 커스텀 훅을 통한 코드 재사용성을 높였습니다.

- **웹 스토리지(Web Storage) 기반 데이터 보존**: 별도의 백엔드 없이 `localStorage`를 활용하여 브라우저 내에 데이터를 안전하게 저장하고 불러오는 데이터 영속성을 구현했습니다.
  - **동기적 초기화**: `useReducer`의 초기화 함수(`init`)를 사용하여 앱 시작 시점에 로컬 스토리지 데이터를 즉시 반영함으로써 로딩 깜빡임 현상을 제거했습니다.
  - **상태 동기화**: `useEffect`를 통해 상태가 변경될 때마다 자동으로 스토리지에 `JSON.stringify` 형태로 데이터를 업데이트하는 로직을 구축했습니다.

배포 주소 : [감정 일기장](https://emotion-diary-rust-five.vercel.app)
